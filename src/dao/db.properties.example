# Copy to db.properties and fill in your local values
# You can also use environment variables: DB_URL, DB_USER, DB_PASSWORD

db.url=jdbc:mysql://localhost:3306/pharmacy?useSSL=false
db.user=root
db.password=your_password_here
